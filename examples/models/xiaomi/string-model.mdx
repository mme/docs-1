---
title: "Xiaomi MiMo String Model"
description: "Create a MiMo agent without importing the model class, using the `model=\"xiaomi:<model-id>\"` string shorthand."
source: cookbook/90_models/xiaomi/string_model.py
---

```python string_model.py
"""
Xiaomi MiMo String Model
========================

Create a MiMo agent without importing the model class, using the
`model="xiaomi:<model-id>"` string shorthand.
"""

from agno.agent import Agent

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(model="xiaomi:mimo-v2.5-pro", markdown=True)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    agent.print_response(
        "Explain why tool-calling agents need conversation history.",
        stream=True,
    )
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Export your Xiaomi MiMo API key">
    <CodeGroup>
    ```bash Mac/Linux
    export MIMO_API_KEY="your_mimo_api_key_here"
    ```

    ```bash Windows
    $Env:MIMO_API_KEY="your_mimo_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `string_model.py`, then run:
    ```bash
    python string_model.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/xiaomi/string_model.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/xiaomi/string_model.py)
