---
title: "OpenAI Reasoning Effort"
description: "Set xhigh reasoning effort and request an automatic reasoning summary from OpenAI Responses."
source: cookbook/90_models/openai/responses/reasoning_effort.py
---

```python reasoning_effort.py
"""
Openai Reasoning Effort
=======================

Cookbook example for `openai/responses/reasoning_effort.py`.
"""

from agno.agent import Agent
from agno.models.openai import OpenAIResponses

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

# reasoning_effort sets how much the model thinks before it answers. OpenAI names
# "none", "minimal", "low", "medium", "high", "xhigh" and "max", and each model page
# lists the ones that model takes. reasoning_summary asks for a summary of the thinking.
agent = Agent(
    model=OpenAIResponses(
        id="gpt-5.5", reasoning_effort="xhigh", reasoning_summary="auto"
    ),
    markdown=True,
)

agent.print_response(
    "Three switches downstairs, one bulb upstairs. One trip up. Which switch?",
    stream=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Export your OpenAI API key">
    <CodeGroup>
    ```bash Mac/Linux
    export OPENAI_API_KEY="your_openai_api_key_here"
    ```

    ```bash Windows
    $Env:OPENAI_API_KEY="your_openai_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `reasoning_effort.py`, then run:
    ```bash
    python reasoning_effort.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/openai/responses/reasoning_effort.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/openai/responses/reasoning_effort.py)
