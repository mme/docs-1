---
title: "Tool Use"
description: "Give a local LlamaCpp model web search tools and stream the tool-assisted answer."
source: cookbook/90_models/llama_cpp/tool_use.py
---

```python tool_use.py
"""Run `uv pip install ddgs` to install dependencies."""

from agno.agent import Agent
from agno.models.llama_cpp import LlamaCpp
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=LlamaCpp(id="ggml-org/gpt-oss-20b-GGUF"),
    tools=[WebSearchTools()],
    markdown=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Whats happening in France?")

    # --- Sync + Streaming ---
    agent.print_response("Whats happening in France?", stream=True)
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs openai
    ```
  </Step>

  <Step title="Install llama.cpp">
    Install the `llama-server` binary. This command supports macOS and Linux with Homebrew; see the [llama.cpp installation guide](https://github.com/ggml-org/llama.cpp/blob/master/docs/install.md) for other platforms:
    ```bash
    brew install llama.cpp
    ```
  </Step>

  <Step title="Start llama.cpp">
    Serve `ggml-org/gpt-oss-20b-GGUF` at `http://127.0.0.1:8080/v1`:
    ```bash
    llama-server -hf ggml-org/gpt-oss-20b-GGUF --ctx-size 0 --jinja -ub 2048 -b 2048
    ```
  </Step>

  <Step title="Run the example">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/llama_cpp/tool_use.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/llama_cpp/tool_use.py)
