---
title: "TrustedRouter Basic"
description: "Run TrustedRouter in synchronous, asynchronous, and streaming modes."
source: cookbook/90_models/trustedrouter/basic.py
---

```python basic.py
"""
TrustedRouter Basic
===================

Cookbook example for `trustedrouter/basic.py`.
"""

import asyncio

from agno.agent import Agent
from agno.models.trustedrouter import TrustedRouter

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=TrustedRouter(),
    markdown=True,
)

# Print the response in the terminal

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("write a two sentence horror story", stream=True)

    # --- Sync + Streaming ---
    agent.print_response("write a two sentence horror story", stream=True)

    # --- Async ---
    asyncio.run(agent.aprint_response("write a two sentence horror story", stream=True))

    # --- Async + Streaming ---
    asyncio.run(agent.aprint_response("write a two sentence horror story", stream=True))
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Export your API keys">
    <CodeGroup>
    ```bash Mac/Linux
    export TRUSTEDROUTER_API_KEY="your_trustedrouter_api_key_here"
    ```

    ```bash Windows
    $Env:TRUSTEDROUTER_API_KEY="your_trustedrouter_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `basic.py`, then run:
    ```bash
    python basic.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/trustedrouter/basic.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/trustedrouter/basic.py)
