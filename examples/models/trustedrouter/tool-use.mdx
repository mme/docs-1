---
title: "TrustedRouter Tool Use"
description: "Give a TrustedRouter agent WebSearchTools and run it in sync and async streaming modes."
source: cookbook/90_models/trustedrouter/tool_use.py
---

```python tool_use.py
"""
TrustedRouter Tool Use
======================

Cookbook example for `trustedrouter/tool_use.py`.
"""

import asyncio

from agno.agent import Agent
from agno.models.trustedrouter import TrustedRouter
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=TrustedRouter(),
    tools=[WebSearchTools()],
    markdown=True,
)

# Print the response in the terminal

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Whats happening in France?", stream=True)

    # --- Sync + Streaming ---
    agent.print_response("Whats happening in France?", stream=True)

    # --- Async ---
    asyncio.run(agent.aprint_response("Whats happening in France?", stream=True))

    # --- Async + Streaming ---
    asyncio.run(agent.aprint_response("Whats happening in France?", stream=True))
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs openai
    ```
  </Step>

  <Step title="Export your API keys">
    <CodeGroup>
    ```bash Mac/Linux
    export TRUSTEDROUTER_API_KEY="your_trustedrouter_api_key_here"
    ```

    ```bash Windows
    $Env:TRUSTEDROUTER_API_KEY="your_trustedrouter_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/trustedrouter/tool_use.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/trustedrouter/tool_use.py)
