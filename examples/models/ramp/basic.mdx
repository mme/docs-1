---
title: "Router Basic"
description: "Run RampRouter directly and through a model string in sync, async, and streaming modes."
source: cookbook/90_models/ramp/basic.py
---

```python basic.py
"""
Router Basic
============

Cookbook example for `ramp/basic.py`.
"""

import asyncio

from agno.agent import Agent
from agno.models.ramp import RampRouter

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=RampRouter(id="gpt-5.6-luna"),
    markdown=True,
)

# The same model can be selected with a model string
agent_from_string = Agent(
    model="ramp:gpt-5.6-luna",
    markdown=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("write a two sentence horror story")

    # --- Sync + Streaming ---
    agent.print_response("write a two sentence horror story", stream=True)

    # --- Async ---
    asyncio.run(agent_from_string.aprint_response("write a two sentence horror story"))

    # --- Async + Streaming ---
    asyncio.run(
        agent_from_string.aprint_response(
            "write a two sentence horror story", stream=True
        )
    )
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Export your API keys">
    <CodeGroup>
    ```bash Mac/Linux
    export RAMP_ROUTER_API_KEY="your_ramp_router_api_key_here"
    ```

    ```bash Windows
    $Env:RAMP_ROUTER_API_KEY="your_ramp_router_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `basic.py`, then run:
    ```bash
    python basic.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/ramp/basic.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/ramp/basic.py)
