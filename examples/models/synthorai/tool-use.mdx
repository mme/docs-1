---
title: "Synthorai Tool Use"
description: "Give a Synthorai agent WebSearchTools and stream a current-events response."
source: cookbook/90_models/synthorai/tool_use.py
---

```python tool_use.py
"""
Synthorai Tool Use
==================

Cookbook example for `synthorai/tool_use.py`.
"""

from agno.agent import Agent
from agno.models.synthorai import Synthorai
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=Synthorai(id="claude-opus-5"),
    markdown=True,
    tools=[WebSearchTools()],
)

agent.print_response("What is happening in France?", stream=True)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    pass
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs openai
    ```
  </Step>

  <Step title="Export your API keys">
    <CodeGroup>
    ```bash Mac/Linux
    export SYNTHORAI_API_KEY="your_synthorai_api_key_here"
    ```

    ```bash Windows
    $Env:SYNTHORAI_API_KEY="your_synthorai_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/synthorai/tool_use.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/synthorai/tool_use.py)
