---
title: "Synthorai Basic"
description: "Generate short prose with Synthorai in synchronous and streaming modes."
source: cookbook/90_models/synthorai/basic.py
---

```python basic.py
"""
Synthorai Basic
===============

Cookbook example for `synthorai/basic.py`.
"""

from agno.agent import Agent
from agno.models.synthorai import Synthorai

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(model=Synthorai(id="claude-opus-5"), markdown=True)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Share a 2 sentence horror story.")

    # --- Sync + Streaming ---
    agent.print_response("Share a 2 sentence horror story.", stream=True)
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno openai
    ```
  </Step>

  <Step title="Export your API keys">
    <CodeGroup>
    ```bash Mac/Linux
    export SYNTHORAI_API_KEY="your_synthorai_api_key_here"
    ```

    ```bash Windows
    $Env:SYNTHORAI_API_KEY="your_synthorai_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `basic.py`, then run:
    ```bash
    python basic.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/synthorai/basic.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/synthorai/basic.py)
