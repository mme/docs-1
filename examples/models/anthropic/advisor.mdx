---
title: "Anthropic Advisor"
description: "Use Anthropic's advisor tool beta with Claude Sonnet and an Opus advisor model."
source: cookbook/90_models/anthropic/advisor.py
---

```python advisor.py
"""
Anthropic Advisor
===============

Cookbook example for `anthropic/advisor.py`.
"""

from agno.agent import Agent
from agno.models.anthropic import Claude

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=Claude(id="claude-sonnet-4-6", betas=["advisor-tool-2026-03-01"]),
    tools=[
        {
            "type": "advisor_20260301",
            "name": "advisor",
            "model": "claude-opus-4-6",
        }
    ],
)


# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Explain how yosemite was underground at one point in time")
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno anthropic
    ```
  </Step>

  <Step title="Export your Anthropic API key">
    <CodeGroup>
    ```bash Mac/Linux
    export ANTHROPIC_API_KEY="your_anthropic_api_key_here"
    ```

    ```bash Windows
    $Env:ANTHROPIC_API_KEY="your_anthropic_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Run the example">
    Save the code above as `advisor.py`, then run:
    ```bash
    python advisor.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/anthropic/advisor.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/anthropic/advisor.py)
