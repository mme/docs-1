---
title: "vLLM Tool Use"
description: "Add web search tools to a vLLM agent and stream responses sync and async."
source: cookbook/90_models/vllm/tool_use.py
---

```python tool_use.py
"""Build a Web Search Agent using xAI."""

import asyncio

from agno.agent import Agent
from agno.models.vllm import VLLM
from agno.tools.websearch import WebSearchTools

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(
    model=VLLM(
        id="NousResearch/Nous-Hermes-2-Mistral-7B-DPO", top_k=20, enable_thinking=False
    ),
    tools=[WebSearchTools()],
    markdown=True,
)

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Whats happening in France?")

    # --- Sync + Streaming ---
    agent.print_response("Whats happening in France?", stream=True)

    # --- Async + Streaming ---
    asyncio.run(agent.aprint_response("Whats happening in France?", stream=True))
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno ddgs openai
    ```
  </Step>

  <Step title="Export your vLLM API key">
    <CodeGroup>
    ```bash Mac/Linux
    export VLLM_API_KEY="your_vllm_api_key_here"
    ```

    ```bash Windows
    $Env:VLLM_API_KEY="your_vllm_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Install vLLM">
    Install vLLM in the environment that will serve the model:
    ```bash
    uv pip install -U vllm
    ```
  </Step>

  <Step title="Start vLLM">
    Serve the model used by this example with automatic tool calling enabled:
    ```bash
    vllm serve NousResearch/Nous-Hermes-2-Mistral-7B-DPO --enable-auto-tool-choice --tool-call-parser hermes
    ```
  </Step>

  <Step title="Run the example">
    Save the code above as `tool_use.py`, then run:
    ```bash
    python tool_use.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/vllm/tool_use.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/vllm/tool_use.py)
