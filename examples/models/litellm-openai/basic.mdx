---
title: "LiteLLM OpenAI Basic"
description: "Run GPT-4o through a LiteLLM proxy server with sync and streaming responses."
source: cookbook/90_models/litellm_openai/basic.py
---

```python basic.py
"""
Litellm Openai Basic
====================

Cookbook example for `litellm_openai/basic.py`.
"""

from agno.agent import Agent, RunOutput  # noqa
from agno.models.litellm import LiteLLMOpenAI

# ---------------------------------------------------------------------------
# Create Agent
# ---------------------------------------------------------------------------

agent = Agent(model=LiteLLMOpenAI(id="gpt-5.6-luna"), markdown=True)

# Get the response in a variable
# run: RunOutput = agent.run("Share a 2 sentence horror story")
# print(run.content)

# Print the response in the terminal

# ---------------------------------------------------------------------------
# Run Agent
# ---------------------------------------------------------------------------
if __name__ == "__main__":
    # --- Sync ---
    agent.print_response("Share a 2 sentence horror story")

    # --- Sync + Streaming ---
    agent.print_response("Share a 2 sentence horror story", stream=True)
```

## Run the Example

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="Install dependencies">
    ```bash
    uv pip install -U agno "litellm[proxy]" openai
    ```
  </Step>

  <Step title="Export your API keys">
    <CodeGroup>
    ```bash Mac/Linux
    export LITELLM_API_KEY="your_litellm_api_key_here"
    export OPENAI_API_KEY="your_openai_api_key_here"
    ```

    ```bash Windows
    $Env:LITELLM_API_KEY="your_litellm_api_key_here"
    $Env:OPENAI_API_KEY="your_openai_api_key_here"
    ```
    </CodeGroup>
  </Step>

  <Step title="Start LiteLLM">
    Start the local OpenAI-compatible proxy on port 4000:
    ```bash
    litellm --model gpt-4o --host 127.0.0.1 --port 4000
    ```
  </Step>

  <Step title="Run the example">
    Save the code above as `basic.py`, then run:
    ```bash
    python basic.py
    ```
  </Step>
</Steps>

Full source: [cookbook/90_models/litellm_openai/basic.py](https://github.com/agno-agi/agno/blob/v3.0.4/cookbook/90_models/litellm_openai/basic.py)
