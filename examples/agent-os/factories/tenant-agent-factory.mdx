---
title: "Per-tenant Agent Factory"
description: "Migrate the legacy tenant-agent factory route to the current Agno v3 factory example."
---

The v2 cookbook source for this route was replaced in Agno v3.

Use [Build an Agent per Request](/examples/agent-os/factories/basic) for the current `AgentFactory` pattern. The factory owns the public agent ID and database while creating a fresh tenant-aware agent for each run.
